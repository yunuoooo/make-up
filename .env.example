# Pi Agent 运行时。Agent 的行为来自 xiaohongshu-makeup-advisor-latest/ 技能文件，
# 这里只配置模型凭据；pi 是 package.json 的依赖，无需全局安装。
DEEPSEEK_API_KEY=""
PI_PROVIDER="deepseek"
PI_MODEL="deepseek-chat"

# 可选覆盖。默认值：PI_BIN=./node_modules/.bin/pi，PI_CODING_AGENT_DIR=./.local-data/pi，
# PI_SKILL_PATH=./xiaohongshu-makeup-advisor-latest
# PI_BIN=""
# PI_CODING_AGENT_DIR=""
# PI_SKILL_PATH=""

# 小红书取数。正路是 TikHub：字段、两层信封与计费陷阱见 docs/specs/09-24-tikhub-xhs-ssot.md
# （**换过供应商**：09-24-justoneapi-xhs-ssot.md 已作废），架构与改动面见 docs/specs/09-24-xhs-api-integration.md。
# .env.example 里的默认值仍是本地 MCP——那是迁移期回退路径；本项目实际部署用的是 api。
# mode 取值：api | mcp | mock（mock 与未知值都按「没有数据源」降级，不发请求也不伪装成真实来源）
XHS_SOURCE_MODE="mcp"

# TikHub（小红书取数的供应商，与淘宝的 Just One 不是一家）。
# token 走请求头 `Authorization: Bearer`，不进 URL。
XHS_API_BASE_URL="https://api.tikhub.io"
# TikHub 用户中心 → API 令牌 里创建。留空即未配置：不发任何请求，链路整体降级，
# 答案会如实说明本轮没有实时站内检索。
XHS_API_TOKEN=""
# 上游建议 120s（至少 60s）；这里先取文档下限，实测 P95 后收紧。
XHS_API_TIMEOUT_SECONDS="60"
# 一轮取数的累计上游耗时预算（不含模型思考时间）：超了就停止发起新请求，已拿到的照发。
XHS_API_BUDGET_SECONDS="60"
# 搜索翻页上限，每页 20 条。
XHS_API_SEARCH_PAGES="2"
# 详情篇数上限，也就是「一轮读几篇正文」——唯一的省钱杠杆。
# 取技能要求的 6–10 篇的上界：每篇是一次计费调用，调小它就少花钱、答案样本也少。
XHS_API_DETAIL_LIMIT="10"

# 小红书 MCP 服务（**迁移期回退路径**，本机浏览器驱动，由 scripts/xhs-mcp-server 启动；
# 验收切到 api 之后整体删除，见 spec 第 10 节的 Phase D）
XHS_MCP_URL="http://127.0.0.1:18060/mcp"
XHS_MCP_AUTH_TOKEN=""
XHS_MCP_REQUEST_TIMEOUT_SECONDS="45"
XHS_MCP_PORT="18060"
XHS_MCP_HEADLESS="true"

# 二进制默认按 <os>-<arch> 从 xiaohongshu-mcp/bin/ 选择，可显式覆盖：
# XHS_PLATFORM="linux-amd64"
# XHS_MCP_BINARY=""
# XHS_LOGIN_BINARY=""
# XHS_DATA_DIR=""

# 淘宝商品数据（Just One API 聚合中转）。字段、错误码和映射规则见
# docs/specs/09-21-justoneapi-taobao-ssot.md；token 在 https://docs.justoneapi.com 控制台获取。
#
# 总开关，默认关。淘宝是采集类接口、按次计费，一轮妆容要打好几次搜索和详情；
# 打开（"true" 或 "1"）才会发请求。这个开关和 token 是「与」的关系：
# 两个都满足才调用，所以配了 token 也不会自动开始烧钱。
TAOBAO_CARDS_ENABLED="false"
# TAOBAO_API_TOKEN 留空即视为未配置：不发任何请求，答案照常，只是没有商品卡片。
TAOBAO_API_BASE_URL="https://api.justoneapi.com"
TAOBAO_API_TOKEN=""
# 上游是采集类接口，官方建议超时 120s（至少 60s）；这里 30s + 下面整批预算 60s。
TAOBAO_API_TIMEOUT_SECONDS="30"
# 一轮答案补全商品卡片的整批预算；超预算只影响没拿到的那几件，已拿到的照发。
TAOBAO_CARDS_BUDGET_SECONDS="60"
TAOBAO_CARD_LIMIT="8"
# 卡片缓存（.local-data/taobao-cards.json）的 TTL，秒；缓存是为了不重复烧调用配额。
TAOBAO_CACHE_TTL_SECONDS="86400"
# 这里曾有一个 TAOBAO_API_MODE="mock"（用 fixture 渲染卡片看 UI），从未实现，已删除。

# Langfuse 全链路 trace（阶段耗时 + 每阶段的 input/output）。
# 两个 key 都为空 = 完全不上报：不发请求、不报错，与 TAOBAO_API_TOKEN 的降级约定一致。
# key 在 https://cloud.langfuse.com 的项目设置里生成。
LANGFUSE_PUBLIC_KEY=""
LANGFUSE_SECRET_KEY=""
LANGFUSE_BASE_URL="https://cloud.langfuse.com"
LANGFUSE_TRACING_ENVIRONMENT="development"
# 建议填 git sha，便于 trace 与发布对应。
# LANGFUSE_RELEASE=""
# 内容开关：false 时只记形状与字节数（字数、字节数、码值），不记正文。
LANGFUSE_TRACE_INCLUDE_CONTENT="true"
